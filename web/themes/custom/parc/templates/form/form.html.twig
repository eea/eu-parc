{#
/**
 * @file
 * Parc implementation for a 'form' element.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The child elements of the form.
 *
 * @see template_preprocess_form()
 *
 * @ingroup themeable
 */
#}

{# {{ attach_library('part/forms') }} #}

{% block form %}
  <form{{attributes}}>
    {{ children }}
  </form>
{% endblock %}
