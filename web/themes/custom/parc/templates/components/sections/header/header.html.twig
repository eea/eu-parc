{#
/**
 * @file
 * Parc Component.
 */
#}

{{ attach_library('parc/navbar') }}
{{ attach_library('parc/nav') }}
{{ attach_library('edwt/collapse') }}
{{ attach_library('parc/s--header') }}

{% set header_attribute = create_attribute() %}

{# On the front page the header sits on top of the grey hero section, so it
   carries the same background until it sticks. #}
{% set header_classes = [
  'sticky-top',
  is_front ? 'bg-gray' : 'bg-white',
  is_front ? 'header--over-hero',
] %}

<header{{header_attribute.setAttribute('id','header').addClass(header_classes)}}>
  <nav class="navbar">
    <div class="container-fluid position-relative">
      {{ page.branding }}

      <button class="navbar-toggler collapsed ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#mainSidebar" aria-controls="mainSidebar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse flex-column" id="mainSidebar">
        {{ page.nav_additional }}
        {{ page.nav_main }}
      </div>
      <div class="nc-bg"></div>
    </div>
  </nav>

  {{ page.header }}
</header>
